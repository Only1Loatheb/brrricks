[package]
name = "type_process_builder"
version = "0.1.0"
edition = "2021"

[dependencies]
process_builder_common = { path = "../process_builder_common" }
serde = { version = "1.0", features = ["derive"] }
anyhow = "1.0.79" # should be this error
frunk_core = "0.4.3"
serde_json = "1.0.134" # remove when I can make it generic over format
